$(document).ready(function(){

    // ici, le 'text' était désactivé par css avec un display none
    // but de l'exercice : le faire réapparaitre avec la méthode '.show()'
    $('.btn').click(function(){
        $('#text').show()
    })
})

// OK